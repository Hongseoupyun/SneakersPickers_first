import React from "react";

export default function MyListingsAddCard() {
  return (
    <div className="addlisting-card">
      will add plus image and will redirect users to add a listing page
    </div>
  )
}